import 'package:get/get.dart';

/*class DisplayReportsController extends GetxController {
  // Dummy data for demonstration purposes
  var reports = <Report>[
    Report(title: 'Daily Revenue', value: '\$500'),
    Report(title: 'Weekly Occupancy Rate', value: '70%'),
    Report(title: 'Monthly Expenses', value: '\$2000'),
    // Add more dummy reports as needed
  ].obs;

  // Method to fetch reports for a specific user
  void fetchReports(String userId) {
    // Here you can implement the logic to fetch reports from your backend
    // Replace this with actual API calls or database queries
    // For demonstration, we're just fetching dummy data
    // You can use asynchronous methods here if needed
    // For example:
    // await _apiService.fetchReports(userId);

    // Clear existing reports
    reports.clear();

    // Add fetched reports to the list
    // For demonstration, we're just adding dummy reports
    reports.addAll([
      Report(title: 'Daily Revenue', value: '\$500'),
      Report(title: 'Weekly Occupancy Rate', value: '70%'),
      Report(title: 'Monthly Expenses', value: '\$2000'),
      // Add more fetched reports as needed
    ]);
  }
}

class Report {
  final String title;
  final String value;

  Report({required this.title, required this.value});

}*/
